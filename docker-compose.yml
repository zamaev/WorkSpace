services:
  workspace:
    build: .
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "/workspace", "health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
